1        # config format version (must be 1)
8192     # ring buffer bytes. Do not raise: malloc'd from a 30000 byte FreeRTOS
         # heap, and failure asserts at boot. On drops, cut events instead.
log      # log file name prefix
0        # enable logging on startup (0/1); 0 -> start with: param set usd.logging 1

# LH+TDoA -> TDoA-only handover capture. Pair with tdoa3_lh_fusion.conf.
# Not config_tdoa_candidates.txt: lighthouse.x/y/z read 0.0 on a fused build.

# Lighthouse sweep angles. Geometry and calibration are not in the log; capture
# them in the same session with: cfcli -u "$CF_URI" lh config read -o lh.yaml
on:estSweepAngle

# All TDoA anchor-pair candidates (needs param tdoaEngine.logCand > 0).
on:estTdoaCand

# Baseline: the TDoA measurement the live algorithm selected.
on:estTDOA

# Flight sequence markers, when examples/app_tdoa_flight is built in.
on:tdoaFlightStep

# IMU, needed to drive the Kalman replay offline.
on:estAcceleration
acc.x
acc.y
acc.z
on:estGyroscope
gyro.x
gyro.y
gyro.z

# Live estimate: seeds the replay and is the replay-vs-flight reference.
on:fixedFrequency
50       # Hz
1        # mode: 1 = synchronous with stabilizer loop
stateEstimate.x
stateEstimate.y
stateEstimate.z
